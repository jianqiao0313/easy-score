# 音色调整与重构检查（2026-09-09）

## 范围与实施顺序

1. 基线：现有 59 项自动化测试全部通过。
2. 默认音色改为钢琴：集中音色定义，统一播放器和偏好设置的默认值，更新 HTML 初始选中项。保留用户主动保存的萨克斯选择。
3. 萨克斯：核实采样来源与播放处理，先补充回归测试，再调整采样和必要的播放逻辑。保持暂停、定位、音色切换和钢琴延音行为。
4. 验证：运行完整测试、构建、语法检查与浏览器播放检查；独立复查改动。
5. 非音频模块仅记录有代码证据的优化建议，本次不进行全项目结构改写。

不新增运行时依赖。音色资源若有替换，保留固定上游版本、许可、署名及文件校验和。

## 本次结果

- 默认钢琴：播放器与偏好设置共用 `src/instruments.mjs` 的定义；HTML 初始选中项同步。用户已保存的萨克斯偏好继续生效。
- 萨克斯：替换为固定版本的 tonejs-instruments / Karoryfer 来源 32 个 MP3，保留录音变化。上游未证明具体型号，因此界面使用“萨克斯”。完整来源见 `public/soundfonts/saxophone/SOURCE.md`。
- 萨克斯映射由约 2.5 MB 内嵌音频改为约 4 KB 元数据，音频按乐谱涉及的根音加载。全部 MP3 共 4,658,464 字节，完整资源体积有所增加，单音首次下载量明显减小。
- 删除运行时响度修正与波形循环搜索；循环点由 `scripts/prepare-saxophone.py` 离线生成，在持续段的同向零交叉处匹配波形。保留原始 MP3 字节。
- 修正长音定位：超过录音长度的播放位置映射回循环区间，并计入音高变速，避免定位到样本尾部。
- 采样仍为单层音色，没有模拟真人的力度层、连奏和每次吹奏差异；听感是否符合偏好仍需实际试听。

## 后续优化建议（本次未修改）

以下均为直接代码证据，优先级按潜在影响排列；异常路径未在真实磁盘故障下复现。

| 优先级 | 问题与证据 | 建议与验证方式 |
| --- | --- | --- |
| 高 | `server/app.mjs:231` 的 `drain()` 设置 processing 后缺少外层 finally；开始或结束时 save 抛错会跳过复位。后续识别任务可能卡住。 | 用明确的失败处理和 finally 释放运行状态；补充一次写盘失败后仍能处理后续任务的测试，避免无界重试。 |
| 中 | `src/main.js:239` 的旧任务循环只屏蔽 UI 更新，切换历史乐谱或上传新文件后仍每秒请求旧任务。 | 用请求身份终止过期轮询，并取消在途请求；验证快速连续导入只保留当前轮询。 |
| 中 | `src/main.js:347` 在新 OSMD render 成功前释放旧实例并清空容器；重排失败会丢失已有谱面。 | 新容器渲染成功后再替换；验证 render 抛错时原谱面仍可见、播放可继续。 |
| 中 | `server/app.mjs:31` 收集全部上传分块，`:50` 又 Buffer.concat，单文件上限 50 MB；并发峰值会明显放大。 | 流式写临时文件并保留格式/大小校验，或先限制上传并发；用并发大文件测量 RSS 和失败清理。 |
| 中 | `src/main.js:1` 静态导入 OSMD；当前生产主 JS 约 1.45 MB、gzip 391 KB，空页面也需加载。入口文件约 660 行，还同时管理导入、排版和播放 UI。 | 先把谱面渲染器提取为模块并延迟加载 OSMD，随后按导入任务与播放控件拆分。用首屏网络瀑布、首次打开乐谱耗时和取消/重排回归测试验收。 |

优先修复失败处理和请求取消，再做模块拆分；无需更换前端框架。

## 验证与复查

- 最终完整测试：`npm test` 63/63 通过；独立复查结论 PASS，针对性测试 28/28 通过。
- 独立审阅发现新采样音域缩小会触发旧逻辑的整件乐器永久降级；已改为逐音符回退，补充 C2/C4/Bb6 混合、纯超界以及后续 C4 自动恢复的回归测试。混合音色在页面明确提示。
- 浏览器真实 Web Audio 验证：32 个 MP3 均可解码、循环起止有效；录音时长为 4.55–7.24 秒；C4 的 8 秒延音输出有效且 release 后归零。页面钢琴/萨克斯选择与采样播放正常，无控制台错误。
- 生产构建通过；现有 OSMD 主包大于 500 KB 的构建提示仍存在，见上方延迟加载建议。项目没有配置独立的 lint/typecheck 命令，使用 Node 语法检查、Python AST 检查和 git diff --check。
- 本地对比试听页：`http://127.0.0.1:4175/.local/audio-review/comparison.html`。旧版/新版走实际播放器采样路径，播放相同旋律与长音，统一平均响度。仅用于本次本地验证，不包含在生产构建内。
