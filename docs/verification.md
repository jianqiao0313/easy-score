# Verification — 2026-09-07

## Actual score recognition

Input: user-provided `四驱小子betop.pdf`, 1 page, SHA-256 `03a73582d56504d172f62aa6c2c273318c5448bd882ee71a95feec7bb5d380d1`.

The production path is raw PDF upload → Poppler 350 DPI rendering → Audiveris 5.11.0 with English OCR → MusicXML → parsed timeline → local sampled Web Audio. The cached demo was generated by the same HTTP pipeline. No hand-authored or mocked notes are served to the user.

- 39 measures, 125 sounding events after merging ties, 156 quarter-note beats.
- Recognized tempo: 167 BPM. Playback duration: approximately 56.05 seconds.
- Original has two sharps and no printed meter. UI labels the key as `2♯` and explicitly reports the 4/4 assumption.
- Independently checked first four measures, measure 25, and the ending against source image/vector geometry. Live integration assertions cover opening eighth rest, G5 dotted half in measure 3, ties within measure 2 and across 3–4, measure 25's eighth rest, and the ending B5 tied note followed by three silent beats.
- All reference assertions passed in `node scripts/verify-live.mjs`. Served PDF bytes match the original hash.
- A two-page fixture made from the same source passed the actual multi-page TIFF route: 78 measures, 250 sounding events, 312 beats, a single coherent MusicXML export. Both original PDF pages were preserved.

The OCR title reads `<<BeT0p>>`. This is retained as recognizer output. The original PDF is available for checking the accurate Chinese title. Individual notes throughout the whole score have not all been manually certified.

## Browser checks

Used the installed browser-harness/CDP surface against the actual local web app, including actual PDF file input uploads.

- PDF upload progresses to rendered SVG, 39 measures, 167 BPM, 0:56 displayed duration.
- Invalid replacement while a valid job is processing remains an error after the earlier job finishes; prior polling cannot overwrite it.
- Piano and saxophone load local samples. AudioContext runs and emits nonzero waveform data. Observed saxophone RMS was 0.01709; piano playback was also measured nonzero. Pause produced RMS 0.
- Metronome enabled/disabled correctly; scheduled accent frequencies are 1320 Hz and 880 Hz.
- Tempo change to 110 updates duration to 1:25; seek-to-end stops; replay starts; reset restores 0:00.
- Cursor advances with playback. Original PDF tab loads an `application/pdf` document.
- At 390 px viewport, document width is 390 px without horizontal page overflow; score SVG reflows to its 314 px container. Desktop SVG width matches its 844 px container.
- No captured application error or unhandled rejection in the final playback pass.
- Screenshots retained locally in `.local/inspection/app-desktop-final.png` and `app-mobile.png`.

## Automated checks and independent review

`npm test`: **22 passed, 0 failed** in the final run. Coverage includes seek-to-end timer cleanup and immediate replay-position reporting.

The suite covers MusicXML rests, chords, accidentals, backup/forward, ties, pickups, transposition, active meter changes, missing metadata, underfilled bars, audio sample fallback, cancellation, pending instrument switch pause/seek, metronome subdivisions, server validation, queue concurrency, persistence, failure states, and MXL extraction.

`npm run build` succeeds. The OSMD dependency results in an approximately 1.32 MB JavaScript bundle (357 KB gzipped), which triggers a non-blocking Vite size advisory. JavaScript syntax is checked by tests/build and module loading.

Independent review requested and verified fixes for the pending instrument switch race, shorter meter changes, and stale polling after invalid replacement. No unresolved important defect remained in that review.

The app runs locally at `http://127.0.0.1:4173` using the built version. It is not publicly deployed. Recognition remains fallible; unsupported MusicXML navigation and inferred metadata are surfaced to the user.
